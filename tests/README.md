# SIDERETH Contract Tests

This directory is reserved for executable contract tests covering the canonical SIDERETH domain model, schemas, state transitions, authorization, idempotency, evidence integrity, provenance and audit boundaries.

Tests are a release gate. A checklist item is not complete merely because the corresponding contract document exists.
