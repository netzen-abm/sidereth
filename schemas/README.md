# Shared Schemas

Canonical versioned schemas for API payloads, domain events, capability contracts and interoperability. All surfaces consume these contracts rather than defining parallel representations.
