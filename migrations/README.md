# Database Migrations

Schema migrations will be introduced only after the canonical SIDERETH domain schema and persistence strategy are approved.

Historical schemas are reference material and are not automatically migrated into legal-case tables. Any reuse requires explicit mapping, review and verification against the canonical SIDERETH model.
